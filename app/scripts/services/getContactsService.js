'use strict';

/*angular
    .module('contactApp')
    .service('getContactsService', function($http, $q) {
        var deferred = $http.get('../../MOCK_DATA.json');
        return deferred.promise;

        this.getNames = function(){

        }
    })
*/